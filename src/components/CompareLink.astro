---
interface Props {
  slug: string;
}
const { slug } = Astro.props;
const outro = `https://pizzaria-seosemanticoeentidade.vercel.app/${slug}/`;
---
<p><a href={outro} rel="nofollow noopener" target="_blank">Veja o exemplo CERTO deste conceito no site irmão →</a></p>
