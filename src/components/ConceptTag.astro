---
interface Props {
  bloco: "Tradicional" | "Semântico" | "Entidade";
  numero: number;
  nome: string;
}
const { bloco, numero, nome } = Astro.props;
---
<span class="concept-tag">Aula · Bloco {bloco} #{numero} — {nome} (jeito ERRADO)</span>
